#!/bin/sh

. /home/wandsas/lib/stdout-log.sh

boldtext "pool: ${1:-zroot}"
boldtext "name\tused\tavailable"
zpool list -H -o name,alloc,free | grep ${1:-zroot}
boldtext "volumes: ${1:-zroot}"
boldtext "name\tused\tavailable"
zfs list -H -o name,used,avail | grep ${1:-zroot}
