#!/bin/sh

. ~/lib/stdout-log.sh

me=$(basename ${0})

export XBPS_PASSPHRASE=foobar

msg "$me: Singing newest xbps packages."
xbps-rindex --sign-pkg --signedby "wandsas" \
    --privkey $HOME/.ssh/id_void.pem \
    $HOME/void-packages/hostdir/binpkgs/*.xbps
msg "$me: Finished, bye."
