#!/bin/sh

. ~/lib/stdout-log.sh

clear
bold "ZFS encryption: ${1:-zroot}"
zfs get all ${1:-zroot} | egrep '(encryption|keylocation|keyformat)'
[ -n "${1}" ] && sudo zfs load-key -n ${1}

printf "\n"
bold "ZFS subvolumes: ${1:-zroot}"
zfs list -H -o name,used | grep ${1:-zroot}


